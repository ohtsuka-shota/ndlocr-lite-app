# .env.example
# このファイルは git に入れる（テンプレート）
# 実際の値は .env に書く（gitignore 済み）

# Anthropic API キー（必須）
ANTHROPIC_KEY=

# 使用モデル（省略可 → haiku がデフォルト）
LLM_MODEL=claude-haiku-4-5-20251001

# NDLOCR-Lite API URL（docker-compose内では変更不要）
# NDLOCR_URL=http://ndlocr:8080
